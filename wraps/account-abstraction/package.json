{
  "name": "account-abstraction-wrapper",
  "description": "Account abstraction rust wrapper",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "polywrap build",
    "deploy": "polywrap deploy",
    "pretest": "polywrap codegen -m src/__tests__/types/polywrap.yaml -g ./src/__tests__/types/wrap",
    "test": "jest --runInBand --verbose"
 },
  "devDependencies": {
    "@types/jest": "26.0.8",
    "jest": "26.6.3",
    "polywrap": "0.10.0-pre.11",
    "ts-jest": "26.5.4",
    "typescript": "4.9.5"
  }
}
