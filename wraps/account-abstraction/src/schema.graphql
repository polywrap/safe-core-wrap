import { MetaTransactionData, MetaTransactionOptions } from "../../common/schema.graphql"

type Module {
    relayTransaction(
        transaction: MetaTransactionData!,
        options: MetaTransactionOptions!
    ): String!
}
