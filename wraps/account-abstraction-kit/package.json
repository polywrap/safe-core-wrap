{
  "name": "account-abstraction-wrapper",
  "description": "Account abstraction rust wrapper",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "polywrap build",
    "deploy": "polywrap deploy",
    "pretest": "polywrap codegen -m tests/polywrap.app.yaml -g ./tests/it/wrap",
    "test": "cargo test --release"
  },
  "devDependencies": {
    "polywrap": "0.11.3"
  }
}
